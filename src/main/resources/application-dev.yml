server:
  port: 7200
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eStore?allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
